name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 အဆင့် (၁) - Code များကို ရယူခြင်း
        uses: actions/checkout@v4

      - name: ☕ အဆင့် (၂) - Java စနစ် တပ်ဆင်ခြင်း (Android အတွက်)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🐦 အဆင့် (၃) - Flutter နောက်ဆုံးဗားရှင်း တပ်ဆင်ခြင်း
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: ⚙️ အဆင့် (၄) - Android Engine ဖိုင်များ အလိုအလျောက် တည်ဆောက်ခြင်း (Fix)
        run: flutter create --platforms=android --org com.tkr .

      - name: 📦 အဆင့် (၅) - လိုအပ်သော Packages များ သွင်းခြင်း
        run: flutter pub get

      - name: 🛠️ အဆင့် (၆) - APK ဖိုင် တည်ဆောက်ခြင်း (Secrets ချိတ်ဆက်ခြင်း)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: flutter build apk --release --dart-define=SUPABASE_URL="$SUPABASE_URL" --dart-define=SUPABASE_ANON_KEY="$SUPABASE_ANON_KEY"
        
      - name: 📤 အဆင့် (၇) - ထွက်လာသော APK ကို သိမ်းဆည်းပေးခြင်း
        uses: actions/upload-artifact@v4
        with:
          name: Real-Estate-CRM-APK
          path: build/app/outputs/flutter-apk/app-release.apk
          
